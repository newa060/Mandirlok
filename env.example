# ── Already in your .env.local ──────────────────────────────
MONGODB_URI=mongodb+srv://...
JWT_SECRET=your-secret-key
RAZORPAY_KEY_ID=rzp_live_...
RAZORPAY_KEY_SECRET=...
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_live_...

# ── ADD THESE for Contact Form to work ──────────────────────

# Gmail SMTP (easiest option):
# 1. Go to Google Account → Security → App Passwords
# 2. Create an App Password for "Mail"
# 3. Use that 16-char password below (NOT your real Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=yourname@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx    # 16-char App Password from Google

# Where contact form submissions go (can be same as SMTP_USER)
ADMIN_EMAIL=yourname@gmail.com

# ── OPTIONAL: Already should exist for OTP ───────────────────
# These are the same SMTP vars — if you already have them for OTP,
# just add ADMIN_EMAIL and you're done.
